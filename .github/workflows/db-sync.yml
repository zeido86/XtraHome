name: Database Sync

on:
  workflow_dispatch:
    inputs:
      run_seed:
        description: "Kör seed efter db push"
        required: true
        default: true
        type: boolean

jobs:
  db-sync:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      - name: Push Prisma schema
        run: npm run db:push -- --accept-data-loss

      - name: Seed database
        if: ${{ inputs.run_seed }}
        run: npm run db:seed
